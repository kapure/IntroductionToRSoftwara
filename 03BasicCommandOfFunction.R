x=1:10
mode(x) # gives "type"/"mode" of variable,here is "numeric"
min(x) #finding minimum
max(x) #finding Maximum
sum(x) #calcutating total sum
cumsum(x) #****calculating "Cumulative sum"*****
prod(x) #calculating total product
y=exp(x) #***calculating exponantial of "each turm"****
round(y,2) #***Rounding upto 2 decimal ******
log(x) #log to th base "e"
sin(x);cos(x);tan(x) # tringnometric function